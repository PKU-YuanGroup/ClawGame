{
  "name": "openclaw-battle-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:game-flow": "tsc --pretty false --module es2022 --moduleResolution bundler --target es2022 --allowImportingTsExtensions --rewriteRelativeImportExtensions --outDir .tmp-smoke scripts/smoke-game-flow.ts && node --experimental-specifier-resolution=node .tmp-smoke/worker/scripts/smoke-game-flow.js"
  },
  "dependencies": {
    "@openclaw/game-engine": "file:../packages/game-engine",
    "@openclaw/game-protocol": "file:../packages/game-protocol"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260301.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.33.0"
  }
}
